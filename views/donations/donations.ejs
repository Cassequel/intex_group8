
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Donate | Ella Rises</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/png" href="/images/favicon.png" />
  <link rel="stylesheet" href="/styles/styles.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
</head>
<body>
  <!-- NAVBAR -->
  <%- include('../partials/navbar') %>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-overlay"></div>
    <div class="container hero-content">
      <p class="eyebrow">Fuel the movement</p>
      <h1>Your donation powers confidence, culture, and opportunity.</h1>
      <p class="hero-subtitle">
        Gifts keep programs accessible, fund instruments and art supplies, and open doors for young Latinas to thrive.
      </p>
      <div class="hero-actions">
        <a href="/donAdd" class="btn btn--primary btn--large">Donate now</a>
        <a href="#donate-table" class="btn btn--primary btn--large">View Donations</a>

      </div>
    </div>
  </section>

  <!-- IMPACT -->
  <section id="impact" class="section section--light">
    <div class="container impact-grid">
      <div>
        <h2>Why your support matters</h2>
        <p>Every contribution helps us deliver culturally rooted programs that build belonging and leadership.</p>
        <ul class="checklist">
          <li>Scholarships and fee waivers so no girl is turned away.</li>
          <li>Instruments, costumes, and art supplies for hands-on learning.</li>
          <li>Transportation assistance to rehearsals, workshops, and campus visits.</li>
          <li>Mentor stipends that bring diverse role models into the classroom.</li>
        </ul>
      </div>
      <div class="impact-card">
        <h3>Recent wins your donations made possible</h3>
        <ul class="checklist checklist--light">
          <li>Funded a new mariachi instrument library for beginners.</li>
          <li>Covered travel for 40 students to tour a local university.</li>
          <li>Launched a STEAM workshop series featuring Latina engineers.</li>
          <li>Provided costumes for our Ballet Folklórico performance cohort.</li>
        </ul>
        <div class="impact-actions">
          <a id="donate" href="#donate" class="btn btn--primary btn--full">Give today</a>
        </div>
      </div>
    </div>
  </section>


    <!-- DONATIONS TABLE -->
  <section id="donate-table" class="section section--light">
    <div class="container">
      <div class="section-header">
        <h2>Recent donations</h2>
        <p>Participant, donation_amount, and date of each gift.</p>
      </div>

      <div class="table-card">
        <div class="table-card__header">
          <span>Donations</span>
          <input 
            type="text" 
            id="donation-search" 
            class="search-input" 
            placeholder="Search by name, amount, or date..."
            style="margin-left: auto; padding: 8px 12px; border: 1px solid #ddd; border-radius: 4px; width: 300px;"
          />
        </div>
        <div class="table-card__body">
          <table class="data-table">
            <thead>
              <tr>
                <th>ID</th>
                <th>Participant</th>
                <th>Donation Amount</th>
                <th>Date</th>
                <% if (userLevel === 'M') { %><th>Actions</th><% } %>
              </tr>
            </thead>
            <tbody>
              <% if (donations && donations.length) { %>
                <% donations.forEach((donation) => { %>
                  <tr>
                    <td><%= donation.donation_id %></td>
                    <td>
                      <a href="/donations/<%= donation.donation_id %>">
                        <%= donation.participant_name || ('ID ' + donation.participant_id) %>
                      </a>
                    </td>
                    <td>$<%= donation.donation_amount %></td>
                    <td>
                      <%
                        const d = donation.donation_date;
                        let formattedDate = "";
                        if (d) {
                          formattedDate = d.toISOString ? d.toISOString().slice(0,10) : String(d).slice(0,10);
                        }
                      %>
                      <%= formattedDate %>
                    </td>
                    <% if (userLevel === 'M') { %>
                      <td class="table-actions">
                        <a class="btn btn--outline btn--small" href="/donations/<%= donation.donation_id %>/edit">Edit</a>
                        <form class="inline-form" method="POST" action="/donations/<%= donation.donation_id %>/delete">
                          <button type="submit" class="btn btn--ghost btn--small">Delete</button>
                        </form>
                      </td>
                    <% } %>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr>
                  <td colspan="<%= userLevel === 'M' ? 5 : 4 %>" class="empty-row">No donations found.</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>

  <!-- STORIES -->
  <section id="stories" class="section">
    <div class="container">
      <div class="section-header">
        <h2>What participants are saying</h2>
        <p>Donor support makes moments like these possible.</p>
      </div>
      <div class="events-grid">
        <article class="event-card">
          <p class="event-date">Arts</p>
          <h3>“I found my voice on stage.”</h3>
          <p class="event-description">
            Ballet Folklórico students performed in new costumes funded by donor gifts, building confidence and pride in their heritage.
          </p>
        </article>
        <article class="event-card">
          <p class="event-date">STEAM</p>
          <h3>“I can see myself in tech.”</h3>
          <p class="event-description">
            Donor-backed STEAM workshops connected girls with Latina engineers and hands-on projects that sparked career curiosity.
          </p>
        </article>
        <article class="event-card">
          <p class="event-date">Summit</p>
          <h3>“College feels possible now.”</h3>
          <p class="event-description">
            Transportation assistance and meals at the Ella Rises Summit ensured every participant could attend without barriers.
          </p>
        </article>
      </div>
    </div>
  </section>

  <!-- DONATE CTA -->
  <section class="section section--accent">
    <div class="container enroll-inner">
      <div>
        <h2>Ready to make an impact?</h2>
        <p>Your gift today keeps Ella Rises accessible, inclusive, and transformative.</p>
      </div>
      <a href="/donations" class="btn btn--primary btn--large">Donate now</a>
    </div>
  </section>

  <!-- FOOTER -->
  <footer id="contact" class="site-footer">
    <div class="container footer-grid">
      <div>
        <h3>Ella Rises</h3>
        <p>A nonprofit organization dedicated to helping young women rise through arts, culture, and education.</p>
      </div>
      <div>
        <h4>Contact</h4>
        <p>Email: <a href="mailto:info@ellarises.org">info@ellarises.org</a></p>
      </div>
      <div>
        <h4>Partners</h4>
        <p>We are grateful for the schools, community centers, and sponsors who support our mission.</p>
      </div>
    </div>
    <div class="footer-bottom">
      <p>Ac 2025 Ella Rises. All rights reserved.</p>
    </div>
  </footer>

</body>
  <script>
    // Donation table search filter
    document.addEventListener("DOMContentLoaded", () => {
      const searchInput = document.getElementById("donation-search");
      const tableRows = document.querySelectorAll(".data-table tbody tr");

      searchInput.addEventListener("input", function () {
        const term = this.value.toLowerCase();

        tableRows.forEach((row) => {
          const rowText = row.innerText.toLowerCase();
          if (rowText.includes(term)) {
            row.style.display = "";
          } else {
            row.style.display = "none";
          }
        });
      });
    });
  </script>
  <script>
    // Simple mobile nav toggle
    const navToggle = document.querySelector(".nav-toggle");
    const navLinks = document.querySelector(".nav-links");

    navToggle.addEventListener("click", () => {
      navLinks.classList.toggle("nav-links--open");
      navToggle.classList.toggle("nav-toggle--open");
    });
  </script>
</body>
</body>
</html>
